{
  "name": "@munari/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "munari kernel: holds and handoffs, provenance, arbitration — pure laws over the retained DOM. Zero runtime dependencies (enforced by tests/boundary.test.ts). Repository-internal; never published independently (decisions.md #1).",
  "exports": {
    ".": "./src/index.ts"
  }
}
