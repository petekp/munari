// The thinnest binding. three + @react-three/fiber arrive as PEER
// dependencies when the first binding contract lands — we are
// three-first, and renderer abstraction is banned by the second-system
// guard (decisions.md #1).
export {}
