# @petepetrash/munari

The [root README](../../README.md) is the canonical consumer guide for
`@petepetrash/munari`. The package build copies that exact file into the
npm artifact, so setup and examples have one source of truth.

Implementation rules for this directory remain in the root
[AGENTS.md](../../AGENTS.md).
