// PLACEHOLDER — replaced by the Lab014 port (three-ui@362c5a1
// app/scenes/Lab014.tsx + lab014.css; kernel math/physics arrive from the
// `anamorph` barrel, `corners` becomes a scene-local helper). Exists so
// the scaffold typechecks before the port lands.
import type { ReactNode } from 'react'

export function Lab014App(_props: { chips: ReactNode }) {
  return null
}
